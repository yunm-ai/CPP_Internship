**第 1 章拷打卷**。
先别急着看下面的提示，**直接拿纸写答案**，或者把代码手敲出来。做完再发我，我按“会不会真做”给你逐题判。

---

# 第 1 章拷打卷题目

## 一、基础概念

### 1）

`main` 函数为什么必须有返回值？

`return 0;`、`return -1;` 分别通常表示什么？

### 2）

下面这两句分别属于什么阶段的问题：编译错误、运行错误，还是都不是？

```cpp
cout << x;
```

```cpp
int x = 10 / 0;
```

### 3）

`#include <iostream>` 的作用是什么？
为什么书里有时候写 `std::cout`，有时候又写 `using namespace std;`？

### 4）

`cin`、`cout`、`cerr`、`clog` 分别干什么？
其中哪些更像“正常输出”，哪些更像“错误/日志输出”？

### 5）

写出 C++ 的两种注释形式，并说明各自适合什么场景。

---

## 二、编译与运行

### 6）

你在命令行里有一个源文件 `prog1.cc`，用 GNU 编译器，写出最基本的编译命令。

### 7）

同一个程序，在 Windows 和 UNIX 下，生成的可执行文件名字通常有什么不同？

### 8）

下面这两个命令分别在干什么？

```bash
./a.out
```

```bash
./prog1
```

### 9）

为什么书里强调“程序源文件名的后缀”很重要？
至少举出 3 个常见后缀。

---

## 三、输入输出

### 10）

下面这段程序输出什么？为什么？

```cpp
#include <iostream>
int main() {
    int v1 = 0, v2 = 0;
    std::cin >> v1 >> v2;
    std::cout << "The sum of " << v1 << " and " << v2
              << " is " << v1 + v2 << std::endl;
    return 0;
}
```

输入：

```text
3 5
```

### 11）

`cin >> v1 >> v2;` 这一句里，连续两个 `>>` 是怎么工作的？

### 12）

如果输入的是：

```text
3 a
```

那么程序会怎样？
你不用写特别细，但要说清楚“第一个读进去了没有”“第二个为什么出问题”。

### 13）

写一个程序，提示用户输入两个整数，输出它们的乘积。

---

## 四、注释与风格

### 14）

下面代码里有什么问题？是语法问题还是风格问题？

```cpp
#include <iostream>
int main() {
    int x = 0;
    // 读取x
    std::cin >> x; /* 输入完成后输出 */
    std::cout << x << std::endl;
    return 0;
}
```

### 15）

为什么书里反复强调“格式风格没有唯一标准，但要保持一致”？

---

## 五、控制流：while / for / if

### 16）

下面这段代码会输出什么？为什么？

```cpp
int i = 0;
while (i < 5) {
    std::cout << i << std::endl;
    ++i;
}
```

### 17）

下面代码为什么会死循环？

```cpp
int i = 0;
while (i < 10) {
    std::cout << i << std::endl;
}
```

### 18）

把第 16 题改写成 `for` 语句。

### 19）

`while` 和 `for` 的本质区别是什么？
你要说“适合什么场景”，不要只说“语法不同”。

### 20）

下面代码输出什么？

```cpp
int x = 0;
if (x)
    std::cout << "A";
else
    std::cout << "B";
```

### 21）

再来一题：

```cpp
int x = -1;
if (x)
    std::cout << "A";
else
    std::cout << "B";
```

### 22）

`if (cin >> x)` 这种写法为什么成立？
它和普通的 `if (x > 0)` 是一类东西吗？

---

## 六、书里的经典循环题

### 23）

编写程序，使用 `while` 将 `50` 到 `100` 的整数相加。
要求输出总和。

### 24）

编写程序，使用递减运算符 `--`，按倒序打印 `10` 到 `0`。

### 25）

编写程序，提示用户输入两个整数，打印这两个整数范围内的所有整数。
比如输入 `3 7`，输出 `3 4 5 6 7`。
要求：先判断大小关系，谁小谁大都能处理。

### 26）

输入若干整数，直到输入结束，输出这些数的总和。

### 27）

输入若干整数，输出其中最大的那个。
这个题是重点，很多人会卡在“第一个数怎么处理”。

---

## 七、Sales_item / 书店程序

### 28）

`Sales_item` 是什么？
它和普通的 `int`、`double` 有什么不同？

### 29）

书店程序里，`item.isbn()` 的作用是什么？

### 30）

下面这段逻辑在做什么？请用自己的话解释，不要照抄书上的话。

```cpp
if (total.isbn() == trans.isbn())
    total += trans;
else {
    std::cout << total << std::endl;
    total = trans;
}
```

### 31）

为什么书店程序要把“同一个 ISBN 的记录”放在一起处理？

### 32）

如果输入文件里根本没有任何销售记录，书店程序最后会做什么？

### 33）

书店程序里，为什么要先读第一条记录，再进入 `while` 循环？

---

## 八、代码纠错题

### 34）

下面代码有什么问题？

```cpp
#include <iostream>
int main() {
    int sum = 0;
    int val = 1;
    while (val <= 10)
        sum += val;
        ++val;
    std::cout << sum << std::endl;
    return 0;
}
```

### 35）

下面代码有什么问题？它是语法错还是逻辑错？

```cpp
#include <iostream>
int main() {
    int v1, v2;
    std::cin >> v1 >> v2;
    std::cout << v1 + v2 << std::endl;
}
```

### 36）

下面代码为什么不够稳妥？

```cpp
int total;
if (std::cin >> total) {
    std::cout << total << std::endl;
}
```

---

## 九、综合实战

### 37）

写一个程序，完成下面任务：

输入一串整数，直到输入结束。
输出：

* 总和
* 个数
* 平均值

要求：

* 用 `while (cin >> x)`
* 不能提前知道输入多少个数

### 38）

写一个程序，输入一串整数，输出最大的那个数。
要求：

* 第一个数单独处理
* 后面的数用循环处理

### 39）

写一个程序，输入若干个 `ISBN + 销售记录`，把相同 ISBN 的记录合并后输出。
你不需要完全实现书店程序，但要写出核心思路。

### 40）

最后一题：
请你用一句话概括第一章最重要的东西是什么。

---

